---
name: user_profile
description: Chinese learner-developer building CRUD + payment modules in spzx-parent (Spring Boot + Vue 3/Element Plus); beginner-level, tutorial-driven
type: user
---

## User Profile

- **Role**: Java developer / learner — actively building CRUD modules (brand, categoryBrand, etc.) and Alipay payment flow across the Spring Boot backend (spzx-manager/service-pay) and the Vue 3 + Element Plus admin frontend
- **Language**: Chinese (Simplified) — always respond in Chinese
- **Project**: spzx-parent — e-commerce system following 尚硅谷 (Atguigu) video tutorial, explicitly "只是学习用" (learning only, no production deployment)
- **Skill level**: Beginner — needs one-line principle + concrete steps for framework basics: Feign `Result<T>` requires `.getData()` unwrapping, Vite dev server config (host/port/proxy), which service needs restarting after config changes
- **Learning style**: Works through a tutorial-style project and asks for organized explanations ("帮我整理, 告诉我原因, 解决方法") — wants root cause + solution, not just the fix
- **Environment**: Windows 10 + cmd. IDEA workspace opens only the backend `e:\java\project-zhenxuan\spzx-parent`; the frontend project lives in the **sibling** directory `e:\java\project-zhenxuan\shangpin\spzx-admin` (port 3001) — user once thought a file "didn't exist" because it was outside the IDE workspace, so give full absolute paths when referencing frontend files
- **Behavior**: Prefers to receive instructions/commands rather than having files auto-modified by the assistant
